select b.contest_id , round(((count(b.contest_id)/(SELECT COUNT(*) FROM Users))*100),2) as percentage from users a join register b on a.user_id = b.user_id group by b.contest_id order by percentage desc , b.contest_id asc